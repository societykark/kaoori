import fetch from 'node-fetch';

export default {
  name: 'tiktokdl',
  description: 'Descarga videos de TikTok sin marca',
  async execute({ bot, chatId, args }) {
    if (!args.length) return bot.sendMessage(chatId, '❌ Uso: /tiktokdl [url]');
    const url = args[0];
    await bot.sendChatAction(chatId, 'typing');
    try {
      const res = await fetch(`https://api.lempi.lat/dl/tiktok?url=${encodeURIComponent(url)}&apikey=lem336`);
      const data = await res.json();
      if (data?.download_url) await bot.sendVideo(chatId, data.download_url);
      else await bot.sendMessage(chatId, '❌ No se pudo descargar el video.');
    } catch {
      await bot.sendMessage(chatId, '❌ Error al descargar.');
    }
  }
};